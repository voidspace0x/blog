# VoidSpace0x 블로그 디자인 개선 계획

작성일: 2026-09-19  
상태: 로컬 구현과 주요 검증 완료. 사용자 승인 완료 (2026-09-19).

## 1. 현재 블로그에 대한 이해

Astro 7 기반의 한국어 개인 기록 공간이다. 소개 페이지에 적힌 “개발하다가 든 생각, 종목을 보다가 든 생각, 그냥 든 생각”을 디자인의 출발점으로 삼는다. `/blog`를 기본 경로로 사용하는 GitHub Pages 정적 사이트이며 댓글은 Giscus로 제공한다.

현재 콘텐츠는 다음 3편이다. 모두 게시일이 2026-09-19이고 대표 이미지가 없다.

| 카테고리 | 글 | 콘텐츠 성격 | 디자인에 필요한 지원 |
| --- | --- | --- | --- |
| 잡담 | 첫 글 | 짧은 문단 중심의 시작 인사 | 짧은 글도 자연스럽게 끝나는 여백과 하단 이동 경로 |
| 개발 | 배포 테스트 | 배포 확인 항목과 인라인 코드 | 목록, 코드, 향후 긴 기술 문서를 위한 본문 스타일 |
| 주식 | 엔비디아(NVDA) 간단 분석 | 요약·강점·리스크·메모로 구분된 글 | 소제목 위계, 목록 간격, 향후 표를 담을 수 있는 본문 폭 |

글의 수와 형식을 고려해 **이미지 없이도 완성도 있게 보이는 텍스트 중심 블로그**로 설계한다. 현재의 짧은 개인 기록에 맞춰 시작하고, 긴 개발 글이나 분석 글이 추가되어도 같은 디자인으로 읽을 수 있게 한다.

검토 근거는 `AGENTS.md`와 이를 가리키는 `CLAUDE.md`, 실제 콘텐츠 3편, 페이지·컴포넌트·레이아웃·전역 CSS 및 설정 파일이다. 아래 문제는 소스 검토에 따른 판단이며, 실제 화면의 줄바꿈·대비·배경 레이어 노출은 구현 단계에서 브라우저로 검증한다. 주식 글의 내용이나 투자 판단에 대한 검토는 범위에 포함하지 않는다.

## 2. 현재 디자인의 문제와 개선 목표

| 현재 상태와 근거 | 독자에게 미치는 영향 | 개선 방향 |
| --- | --- | --- |
| 홈과 전체 글 페이지가 모두 모든 글을 같은 카드 목록으로 출력 | 글이 늘어날수록 두 페이지의 역할이 겹침 | 홈은 소개와 최근 글 6편, 전체 글은 모든 기록 탐색으로 구분 |
| 전역 `h3`가 `1.7em`이고 카드 제목이 이를 사용 | 기본 18px 기준 제목이 약 31px로 커서 목록의 밀도가 낮음 | 카드 제목을 22px로 독립 지정하고 요약·날짜와 위계 정리 |
| 헤더는 전체 화면 폭, 주요 페이지는 720px 중심 | 넓은 화면에서 로고·메뉴와 콘텐츠의 기준선이 다름 | 헤더와 목록 페이지에 공통 1040px 컨테이너 적용 |
| 모바일 헤더가 `flex-wrap`에 의존 | 로고와 메뉴의 줄바꿈 위치가 화면 폭에 따라 달라질 수 있음 | 작은 화면은 브랜드와 메뉴를 명시적으로 두 줄 배치 |
| 본문 `main`과 `.prose`가 서로 다른 폭·여백 규칙을 중첩 | 목록에서 글로 이동할 때 정렬과 모바일 여백이 불안정해질 수 있음 | 페이지 컨테이너와 본문 컨테이너의 역할 분리 |
| 별, 성운, 제목 그라데이션, 링크 발광, 카드 이동 효과가 함께 존재 | 제목과 본문보다 장식이 먼저 눈에 들어올 수 있음 | 우주 분위기는 배경과 궤도 마크에 집중하고 본문 주변 효과 절제 |
| 본문 링크의 밑줄이 hover에서만 나타남 | 터치 환경에서 일반 문장과 링크를 구별하기 어려움 | 본문 링크에 기본 밑줄 제공 |
| 카테고리 페이지에 제목과 목록만 존재 | 각 기록의 성격과 현재 탐색 위치를 설명하는 정보가 부족 | 한 줄 설명·글 수·카테고리 탐색 링크 추가 |
| 상세 페이지가 본문 후 바로 댓글로 이어짐 | 글을 다 읽은 뒤 다른 글로 이동할 동선이 약함 | 카테고리로 돌아가기와 다음 읽을 글을 댓글 앞에 배치 |

목표는 “공허를 떠도는 기록들”이라는 개성을 살리면서 **첫 방문에서 블로그의 성격을 이해하고, 글을 고르고, 편하게 읽는 흐름**을 만드는 것이다.

## 3. 디자인 방향: 조용한 우주 속 개인 기록

- 짙은 남색 바탕, 보라와 청록 포인트, 궤도 마크를 브랜드 요소로 사용한다.
- 화면의 주인공은 한국어 제목과 본문이다. 장식은 텍스트 뒤에서 대비를 떨어뜨리지 않도록 배치한다.
- 홈은 왼쪽 정렬 소개와 글 목록으로 구성한다. 오른쪽의 작은 궤도 일러스트로 기존 우주 정체성을 표현한다.
- 이미지가 없는 글에는 빈 이미지 영역이나 자동 생성 썸네일을 만들지 않는다.
- 카테고리는 한글 이름과 색상을 함께 표시한다. 주식은 초록, 개발은 청록, 잡담은 보라를 유지한다.
- 개인 블로그의 규모에 맞춰 추천 배너, 인기 순위, 방문자 통계, 대형 카드 그리드는 도입하지 않는다.

### 색상 토큰 초안

아래 값은 구현 시작값이다. 실제 텍스트와 배경 조합의 대비를 측정한 뒤 확정한다.

| 토큰 | 제안 값 | 용도 |
| --- | --- | --- |
| `--bg` | `#0A0E17` | 전체 배경 |
| `--surface` | `#121927` | 카드·코드 외곽·보조 영역 |
| `--surface-hover` | `#192235` | 카드 hover와 선택 영역 |
| `--border` | `#2A354B` | 구분선·카드 테두리 |
| `--text` | `#E8EDF5` | 제목과 본문 |
| `--text-muted` | `#A0ACC0` | 요약·날짜·보조 설명 |
| `--accent` / `--cat-chat` | `#B29AFF` | 브랜드 보라·잡담 |
| `--accent-2` / `--cat-dev` | `#67DCEC` | 링크·개발 |
| `--cat-stock` | `#6BD9B0` | 주식 |
| `--focus-ring` | `#8DE8F3` | 키보드 포커스 |

카테고리 색상은 기존 `data-category` → `--cat-color` 방식을 사용한다. 일반 텍스트는 배경 대비 4.5:1 이상, 큰 텍스트와 주요 UI 경계·포커스 표시는 3:1 이상을 검증 기준으로 삼는다.

### 타이포그래피와 간격

| 요소 | 데스크톱 | 모바일 | 공통 규칙 |
| --- | --- | --- | --- |
| 홈 메인 문구 | 44px | 30px | 두 줄 이내를 목표로 하고 긴 경우 자연스럽게 확장 |
| 페이지 제목 | 36px | 28px | 굵기 700, 행간 1.3 |
| 글 상세 제목 | 40px | 30px | 행간 1.35, 단어 단위 줄바꿈 |
| 목록 제목 | 22px | 20px | 굵기 650~700, 행간 1.45 |
| 본문 | 18px | 17px | 행간 1.85 |
| 목록 요약 | 16px | 15px | 행간 1.65 |
| 날짜·배지 | 13~14px | 13px | 행간 1.5 |
| 코드 블록 | 14px | 13px | 행간 1.7 |

Pretendard와 JetBrains Mono를 유지한다. 제목과 본문은 한국어에 맞는 `word-break: keep-all`과 긴 URL을 위한 `overflow-wrap: anywhere`를 함께 검토한다. 코드에는 별도 줄바꿈 규칙을 적용한다.

간격은 4·8·12·16·24·32·48·64·80px 단위로 통일한다. 카드 모서리는 12px, 코드 블록은 10px, 배지는 캡슐 형태로 제한한다. 강한 그림자 대신 배경색과 1px 테두리로 층을 구분한다.

## 4. 페이지별 구체적인 구성

### 4.1 공통 헤더와 푸터

데스크톱 헤더는 높이 약 72px, 최대 폭 1040px로 한다. 왼쪽에는 28px 궤도 마크와 `VoidSpace0x`, 오른쪽에는 `홈 / 전체 글 / 소개 / GitHub`를 배치한다. 카테고리 탐색은 목록 페이지의 탐색 줄에서 제공한다. 전체 글 화면으로 가는 경로를 헤더에 명시한다.

767px 이하에서는 첫 줄에 브랜드와 GitHub, 둘째 줄에 `홈 / 전체 글 / 소개`를 배치한다. 메뉴는 항상 보이며 별도의 햄버거 메뉴는 만들지 않는다. 각 링크의 터치 영역은 최소 44px 높이를 확보한다. 헤더는 우선 일반 문서 흐름에 두어 모바일 읽기 영역을 확보한다.

활성 메뉴는 색상·얇은 밑줄·`aria-current="page"`로 표현한다. 글 상세와 카테고리 경로에서는 `전체 글`을 상위 탐색 위치로 표시한다. 경로 비교는 `/blog` 기본 경로를 제거한 뒤 후행 슬래시를 정규화해 처리한다.

푸터는 콘텐츠와 같은 정렬선에 둔다. 저작권, “공허를 떠도는 기록들.”, GitHub·RSS 링크를 표시하고 상하 여백은 32~48px로 줄인다. 짧은 페이지는 `body`의 세로 flex 구조와 `main`의 확장으로 푸터가 화면 하단에 놓이도록 한다.

### 4.2 홈

```text
[궤도 마크 VoidSpace0x]                 홈  전체 글  소개  GitHub

  공허를 떠도는 기록들.                    작은 궤도 일러스트
  개발하며 배운 것, 시장을 보며 든 생각,
  그리고 일상의 작은 메모를 남깁니다.
  [전체 글 보기 →]  [소개]

  최근 기록                                      전체 글 보기 →
  [전체 3] [잡담 1] [개발 1] [주식 1]

  잡담 · 2026.09.19
  첫 글
  VoidSpace0x, 시작합니다.                                   ↗
  ──────────────────────────────────────────────────────────
  개발 · 2026.09.19
  배포 테스트
  글 추가 → push → 자동 배포가 되는지 확인용 글              ↗

  푸터
```

위 글 순서는 배치 예시다. 실제로는 게시일 내림차순을 사용하며 같은 날짜의 글은 `post.id`로 순서를 고정한다. 특정 글을 임의로 대표 글로 승격하지 않는다.

히어로의 주 문구는 기존 사이트 설명을 사용한다. 브랜드 이름은 헤더에서 충분히 보여주므로 홈 제목은 기록 공간의 성격을 전달한다. 소개 문구는 현재 소개 페이지에서 유래한 초안이다.

데스크톱 히어로는 텍스트와 장식의 7:3 비율, 상하 64px 여백으로 구성한다. 장식은 기존 궤도 마크를 CSS 또는 단순 SVG로 확장하고 약 160px 이내로 제한한다. 모바일에서는 장식을 숨기고 상하 32px 여백을 적용한다. 390×844 화면에서 첫 글의 제목과 요약이 첫 화면에 들어오는 것을 목표로 한다.

최근 글은 최대 6편을 노출한다. 현재는 3편만 자연스럽게 출력한다. 카테고리 탐색 줄은 자바스크립트 필터처럼 보이는 버튼이 아닌 실제 링크이며, 선택 시 해당 목록 페이지로 이동한다. 글 수는 표시 대상 콘텐츠에서 계산하고 숫자를 하드코딩하지 않는다.

### 4.3 글 목록 컴포넌트

`PostCard.astro`를 낮은 높이의 가로형 목록 카드로 개선한다. 첫 줄은 카테고리와 날짜, 둘째 줄은 제목, 셋째 줄은 요약으로 배치한다.

- 목록은 한 열을 유지하고 카드는 데스크톱 24px, 모바일 20px 안쪽 여백을 준다.
- 제목은 생략하지 않는다. 요약은 목록에서 최대 두 줄까지만 보여준다.
- 기존 왼쪽 3px 색상 선은 제거하고 배지의 색상으로 구분한다. 카드 배경과 테두리는 은은하게 사용한다.
- 카드 전체를 하나의 링크로 유지한다. 내부에 다른 링크나 버튼을 중첩하지 않는다.
- hover에서는 배경색과 테두리만 160ms 안에 전환하고 카드 이동·강한 발광을 없앤다.
- 키보드로 접근하면 카드 둘레에 2px 포커스 링과 3px 간격을 표시한다.
- 오른쪽 화살표는 장식으로 처리하며 좁은 화면에서는 숨긴다.
- 개발 모드에서 초안이 노출될 때는 `초안` 배지를 함께 표시한다.

### 4.4 전체 글과 카테고리

전체 글 페이지는 `전체 글` 제목, “개발, 시장, 일상을 기록합니다.” 설명, 글 수, 공통 카테고리 탐색 줄, 글 목록 순으로 구성한다. 배포 기준 전체 글 URL은 기본 경로와 페이지 경로가 합쳐진 `/blog/blog/`임을 링크 구현 시 유의한다.

카테고리 페이지는 같은 틀을 사용하고 제목과 설명만 바꾼다.

| 카테고리 | 설명 문구 초안 |
| --- | --- |
| 잡담 | 그때그때 떠오른 생각과 일상의 작은 기록. |
| 개발 | 만들면서 배운 것, 막혔다가 풀린 것. |
| 주식 | 보고 있는 종목과 시장에 대한 생각, 그리고 복기. |

선택된 카테고리에는 색상과 테두리, 현재 위치 속성을 함께 적용한다. 빈 카테고리에는 “아직 기록이 없습니다.”와 전체 글 링크를 제공한다. 지금은 페이지네이션을 추가하지 않으며, 공개 글이 24편을 넘을 때 페이지당 12편 구성을 검토한다.

### 4.5 글 상세

본문 최대 폭은 720px, 모바일 좌우 여백은 20px로 한다. 부모 `main`과 `.prose`의 중복 패딩을 없애 실제 본문 폭이 의도대로 유지되게 한다.

```text
  ← 개발의 다른 기록

  개발 · 2026.09.19
  배포 테스트
  글 추가 → push → 자동 배포가 되는지 확인용 글
  ──────────────────────────────────────────
  본문

  ──────────────────────────────────────────
  다음 읽을 기록              전체 글 보기 →
  [글 제목 / 카테고리 / 날짜]

  댓글
  GitHub 계정으로 이야기를 남길 수 있습니다.
  [Giscus]
```

글 제목과 메타데이터는 왼쪽 정렬한다. `description`은 제목 아래 리드 문장으로 표시하되 본문과 거의 같은 문구가 반복되는 경우에는 생략할 수 있도록 구현 시 검토한다. 수정일은 “수정 2026.09.20”처럼 한국어로 표시한다.

본문 세부 규칙:

- 문단 사이 여백은 약 24px, `h2` 위는 48px·아래는 16px로 한다. 본문 `h2`는 28px/모바일 24px, `h3`는 22px/모바일 20px로 제한한다.
- 목록 항목 사이에는 8px, 중첩 목록에는 12px 여백을 둔다.
- 본문 링크는 기본 밑줄을 표시하고 hover에서 색상을 변경한다.
- 코드 블록은 Dracula 테마를 유지하고 내부에서만 가로 스크롤되게 한다. 코드가 페이지 전체 폭을 늘리지 않아야 한다.
- 표는 헤더 배경·셀 패딩·행 구분선을 제공한다. 넓은 표는 포커스 가능한 별도 가로 스크롤 영역 안에 두고 영역 이름을 제공한다.
- 인용문은 얇은 보라색 선과 은은한 배경을 사용하고 본문보다 크게 만들지 않는다.
- 대표 이미지가 있는 글은 제목 아래에 표시한다. 없는 글에서는 이미지 컨테이너도 출력하지 않는다. 이미지 비율을 보존하고 의미 있는 이미지에는 대체 텍스트를 제공할 방법을 함께 마련한다.

목차는 `h2`가 4개 이상인 글에만 제목 아래 접을 수 있는 `<details>`로 제공한다. Astro의 렌더링 결과에서 heading 정보를 받아 앵커를 만들고, 자바스크립트 없이 사용할 수 있도록 한다. 현재 엔비디아 글로 목차 상태를 확인하고 짧은 잡담·개발 글에서는 숨김 상태를 확인한다.

글 하단에는 같은 카테고리의 다른 최신 글을 최대 2편 표시한다. 해당 글이 없으면 전체 최신 글에서 현재 글을 제외하고 최대 2편 표시한다. 후보가 전혀 없으면 전체 글 링크만 보여준다. 공개 페이지에서 초안이 추천에 섞이지 않게 한다.

Giscus 앞에는 `댓글` 제목과 로그인 안내를 둔다. 기존 저장소·카테고리 ID와 pathname 매핑을 유지해 기존 댓글 연결을 보존한다. 외부 댓글이 로드되지 않아도 글 하단 탐색을 이용할 수 있어야 한다.

### 4.6 소개와 404

소개 페이지는 현재의 개인적인 문체를 살리고 제목 → 짧은 소개 → 세 가지 기록 주제 → GitHub 링크 순으로 구성한다. 사진·이력·프로젝트 실적은 주어진 자료가 없으므로 추가하지 않는다. 주제 소개는 모바일에서 한 열, 데스크톱에서도 본문 폭 안에서 간결한 목록으로 표시한다.

404는 작은 궤도 마크, `404`, “이 페이지는 공허 속으로 사라졌습니다.”, 홈·전체 글 링크로 구성한다. 공통 헤더와 푸터의 정렬·간격을 그대로 사용한다.

## 5. 반응형·배경·접근성

| 화면 폭 | 컨테이너와 배치 |
| --- | --- |
| 320~767px | 좌우 20px, 헤더 두 줄, 홈 장식 숨김, 목록 한 열, 탐색 링크 필요 시 줄바꿈 |
| 768~1199px | 좌우 32px, 헤더 한 줄, 홈 7:3 구성, 목록 한 열 |
| 1200px 이상 | 목록·헤더 최대 1040px 중앙 정렬, 글 본문 최대 720px |

본문 페이지에서는 배경 별과 성운의 불투명도를 홈보다 낮춘다. 현재 별의 좌표가 px 단위로 고정되어 있으므로 작은 화면에서 보이는 밀도를 확인한다. 조정이 필요하면 지침에 따라 전체 box-shadow 목록을 생성 스크립트로 다시 만들고 개별 좌표를 수동 수정하지 않는다. 배경의 음수 z-index는 의도한 적층 맥락 안에서 보이는지 확인하고 장식이 클릭을 가로채지 않게 한다.

모션은 홈의 궤도 마크 한 곳에 집중한다. 헤더의 마크는 정적으로 표시한다. `prefers-reduced-motion`에서는 궤도·별 애니메이션 및 이동 효과를 중단한다.

공통 접근성 요구 사항:

- 본문 바로가기 링크, 페이지별 한 개의 `h1`, 논리적인 제목 계층을 제공한다. 홈 카드 제목은 `최근 기록`의 `h2` 아래 `h3`, 카테고리·전체 글 카드 제목은 페이지 `h1` 아래 `h2`로 조정 가능하게 한다.
- 링크·버튼·스크롤 영역에 키보드 포커스를 표시한다.
- 색상 외에 텍스트와 형태로 선택 상태를 전달한다.
- 200% 확대와 320px 화면에서 페이지 전체의 가로 스크롤이 발생하지 않게 한다.
- 폰트 CDN 로딩이 실패해도 시스템 폰트로 읽을 수 있어야 한다.
- 카드의 전체 높이를 고정하지 않아 긴 한글 제목과 확대 설정을 수용한다.

## 6. 구현 단위와 순서

| 순서 | 작업 | 주요 파일 | 완료 조건 |
| --- | --- | --- | --- |
| 1 · P0 | 토큰·본문 폭·간격·포커스·기본 텍스트 스타일 정리 | `src/styles/global.css` | 목록과 본문에 독립된 폭 규칙 적용, 모바일 중복 여백 제거 |
| 2 · P0 | 헤더·푸터·현재 위치 표시 정리 | `Header.astro`, `HeaderLink.astro`, `Footer.astro` | 320px부터 메뉴 접근 가능, 전체 글 진입점 확보 |
| 3 · P0 | 목록 카드와 공통 카테고리 탐색 구현 | `PostCard.astro`, 신규 `CategoryNav.astro`, `src/consts.ts` | 메타·제목·요약 위계 통일, 현재 카테고리 표시 |
| 4 · P0 | 홈·전체 글·카테고리 페이지 재구성 | `src/pages/index.astro`, `src/pages/blog/index.astro`, `src/pages/category/[category].astro` | 홈 6편 제한, 실제 글 수, 페이지별 설명·빈 상태 적용 |
| 5 · P0 | 본문 타이포그래피와 상세 헤더 개선 | `src/layouts/BlogPost.astro`, `src/styles/global.css` | 현재 세 글이 동일한 읽기 규칙으로 출력 |
| 6 · P1 | 조건부 목차·하단 다른 글·댓글 구획 | `src/pages/blog/[...slug].astro`, `BlogPost.astro`, 필요 시 신규 `TableOfContents.astro`, `RelatedPosts.astro` | heading·현재 글 ID 전달, 초안 제외, 기존 댓글 연결 유지 |
| 7 · P1 | 소개·404·배경 장식 마무리 | `src/pages/about.astro`, `src/pages/404.astro`, `global.css` | 공통 디자인과 정렬 일치, 모션 감소 설정 반영 |
| 8 · P0 | 빌드·경로·반응형·키보드 검증 | 전체 변경 파일 | 아래 검수 항목 충족 |

P0는 첫 디자인 적용에서 완료해야 하는 항목이다. P1은 기본 화면이 정리된 뒤 같은 개선 작업의 후반에 적용한다. 파일 경로가 축약된 컴포넌트는 `src/components/` 기준이다.

페이지에서 중복되는 목록 스타일과 카테고리 설명은 공통 컴포넌트·상수로 이동한다. 전체 페이지 구조를 바꾸는 공통 루트 레이아웃 도입은 이번 디자인 개선의 필수 조건으로 삼지 않는다.

## 7. 구현 시 보존할 규칙과 연관 수정

- 모든 내부 링크는 `BASE_URL.replace(/\/$/, '')`와 페이지 경로를 조합한다. 서버 루트 기준 `/category/...` 링크를 직접 출력하지 않는다.
- 카테고리 enum과 단일 카테고리 모델을 유지한다. 태그나 작성자 시스템을 추가하지 않는다.
- 운영 목록·글 수·관련 글은 초안을 제외한다. RSS의 초안 제외 규칙도 유지한다.
- 콘텐츠 원문과 게시일을 디자인 적용 과정에서 임의로 수정하지 않는다. 배포 테스트 글의 공개 여부는 콘텐츠 관리 단계에서 판단한다.
- `withastro/action`의 Node 22 설정을 유지한다.
- `BaseHead.astro`의 RSS autodiscovery URL은 현재 `new URL('rss.xml', Astro.site)`로 구성되어 `/blog`를 포함하지 않는다. 푸터 RSS와 같은 `/blog/rss.xml`로 맞추는 작은 경로 수정도 링크 검수에 포함한다.
- 대표 이미지 대체 텍스트를 추가한다면 `src/content.config.ts`에 선택 필드를 두어 기존 글과 호환한다. 현재 글에는 새 필수 입력을 요구하지 않는다.
- 새로운 UI 프레임워크나 애니메이션 라이브러리를 추가하지 않는다. 기본 탐색과 읽기는 자바스크립트 없이 동작하도록 한다.

검색·라이트 테마·조회수·코드 복사 버튼은 이번 적용에서 제외한다. 글이 늘어나 실제 탐색이나 읽기 불편이 확인될 때 별도 작업으로 검토한다.

## 8. 검수 계획과 완료 기준

아래는 계획 단계에서 정한 검수 항목이다. 실제 구현 및 수행한 검증 결과는 9절에 기록한다.

### 기능과 콘텐츠

- `npm run build`가 성공한다.
- 미리보기 서버에서 `/blog/`, `/blog/blog/`, `/blog/category/chat/`, `/blog/category/dev/`, `/blog/category/stock/`, `/blog/about/` 및 실제 글 3편 경로를 요청해 정상 응답과 화면 내용을 확인한다.
- 404 출력과 홈·전체 글 복귀 링크를 확인한다. 정적 미리보기와 GitHub Pages의 404 처리 차이는 배포 환경에서 확인한다.
- 로고·메뉴·카테고리·카드·목차·다른 글·RSS 링크가 올바른 대상에 도달한다.
- 임시 초안으로 운영 목록·글 수·관련 글·RSS 제외를 확인한 뒤 검증용 파일을 제거한다.
- 같은 날짜의 글 순서가 홈·전체 글·카테고리에서 일관적이다.

### 화면과 읽기

- 390×844 모바일, 768px 태블릿, 1440×900 데스크톱에서 홈·카테고리·상세 화면을 실제 브라우저로 확인한다. 320px 폭도 별도로 검사한다.
- 모바일 첫 화면에 첫 글 제목과 요약이 보이며, 브랜드 소개가 글 목록을 지나치게 아래로 밀어내지 않는다.
- 짧은 잡담, 목록이 있는 개발 글, 여러 소제목이 있는 주식 글 모두 제목·본문·댓글 구획이 자연스럽다.
- 임시 긴 제목·긴 URL·코드 블록·넓은 표·대표 이미지로 레이아웃이 깨지지 않는지 확인한다. 현재 콘텐츠에 없는 상태는 검수용으로만 만든다.
- 표와 코드의 가로 스크롤이 페이지 전체에 전파되지 않는다.
- 본문 뒤 장식이 읽기를 방해하지 않고 이미지가 없는 카드에 빈 공간이 생기지 않는다.

### 접근성과 로딩

- 마우스 없이 본문 바로가기 → 메뉴 → 글 → 하단 링크 순으로 이동할 수 있다.
- 현재 메뉴·카테고리 표시, 색상 대비, 포커스 표시를 확인한다.
- 200% 확대, 모션 감소 설정, 폰트 로딩 실패 상황에서 읽기와 이동을 유지한다.
- Giscus 지연 또는 차단 상황에서도 본문과 다른 글 링크가 정상적으로 표시된다.
- 장식을 위해 추가된 대형 이미지나 클라이언트 런타임이 없고, 대표 이미지에는 크기 정보가 제공되어 로딩 시 레이아웃 이동을 줄인다.

완료 시에는 변경된 화면의 데스크톱·모바일 캡처와 빌드 결과를 함께 남겨, 위 디자인 명세와 실제 결과를 비교할 수 있게 한다.


## 9. 로컬 구현 결과 (2026-09-19)

- 홈, 전체 글, 카테고리, 글 상세, 소개, 404와 공통 헤더·푸터를 새 디자인으로 구현했다.
- 공통 카테고리 탐색, 일관된 글 정렬, 최근 6편, 목차, 다음 읽을 글, 초안 배지를 적용했다.
- 본문 바로가기와 키보드 포커스, 모션 감소 대응, 대표 이미지 대체 텍스트를 추가했다.
- 넓은 표는 Astro 7의 Sätteri 플러그인으로 빌드 시 스크롤 영역을 생성한다. 브라우저 스크립트는 필요하지 않다. 기존 전이 의존성이던 `@astrojs/markdown-satteri`를 직접 의존성으로 명시했다.
- 새 글 파일명은 저장 시점의 한국 시간 기준 `YYYY-MM-DD-HHmm-제목.md`로 생성한다. 기존 글은 파일명과 주소를 유지한다.
- 운영 초안은 목록·글 수·추천·RSS뿐 아니라 상세 경로 생성에서도 제외한다.
- RSS 메타 링크에 `/blog` 기본 경로를 반영했다. 기존 콘텐츠 원문과 Giscus 연결 설정은 유지했다.

수행한 검증:

- 최종 `npm run build` 성공: 정적 HTML 10페이지 생성.
- 주요 페이지와 RSS 총 10개 경로 HTTP 200, 존재하지 않는 경로 HTTP 404 및 복귀 링크 확인.
- 390px 모바일 홈에서 첫 카드 하단이 약 702px로 844px 높이 첫 화면 안에 표시됨.
- 320px의 긴 제목·URL·코드·표·대표 이미지 임시 글로 좁은 화면 검증. 표와 코드는 내부 가로 스크롤로 처리됨.
- 768px 카테고리와 1440px 홈·상세 화면 확인. 본문 바로가기의 실제 키보드 포커스 이동 확인.
- 목차 펼침, 현재 메뉴·카테고리 표시, Giscus 댓글 UI 로딩 확인.
- 임시 초안이 운영 HTML·RSS에 포함되지 않는 것을 확인하고 검증용 글을 제거한 뒤 재빌드.
- `git diff --check` 통과.

미리보기: `http://127.0.0.1:4321/blog/`  
검토용 화면 캡처: `/tmp/blog-design-review/`  
GitHub 반영: 사용자 승인에 따라 `voidspace0x/blog`의 `main`에 반영한다. 기존 `AGENTS.md`의 사용자 변경은 이번 디자인 커밋에서 제외한다.
